---
description: "Grill the plan, then draft one cycle document"
argument-hint: "[feature summary]"
---

Read `.agents/skills/specify-design/SKILL.md` and execute that shared skill exactly.

Use `$ARGUMENTS` as the current user request for this command.
