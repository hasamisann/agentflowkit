---
description: "Implement one task or one requested wave"
argument-hint: "<task-file> | wave <N> | bug <bug-id> wave <N>"
---

Read `.agents/skills/implement/SKILL.md` and execute that shared skill exactly.

Use `$ARGUMENTS` as the current user request for this command.
