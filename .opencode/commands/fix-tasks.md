---
description: "Break one finalized investigation into fix tasks"
argument-hint: "<investigation path or bug id>"
---

Read `.agents/skills/fix-tasks/SKILL.md` and execute that shared skill exactly.

Use `$ARGUMENTS` as the current user request for this command.
